{
  "name": "bloodtesting-pro",
  "version": "2.4.9",
  "private": true,
  "description": "Medical educational suite for blood determination and genetics",
  "type": "module",
  "scripts": {
    "build": "mkdir -p build-output && cp index.html index.tsx types.ts metadata.json build-output/ && cp -rv components services build-output/",
    "start": "echo 'Ready'"
  },
  "engines": {
    "node": "20"
  },
  "dependencies": {
    "lucide-react": "^0.454.0"
  }
}